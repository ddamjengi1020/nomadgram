# test

type User {
  id: Int!
  firstName: String
  lastName: String
  bio: String
  email: String
  userName: String
}

type Query {
  findUser: [User]
}

type Mutation {
  newUser: User
  deleteAllUser: [User]
}
